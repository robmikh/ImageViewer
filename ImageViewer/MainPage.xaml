<Page
    x:Class="ImageViewer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <local:FloatToPercentageConverter x:Key="FloatToPercentageConverter" />
    </Page.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <controls:TabbedCommandBar x:Name="MainMenu">
            <controls:TabbedCommandBar.PaneFooter>
                <CommandBar Background="Transparent" DefaultLabelPosition="Right">
                    <AppBarButton Label="Help" Icon="Help" />
                </CommandBar>
            </controls:TabbedCommandBar.PaneFooter>
            <controls:TabbedCommandBar.MenuItems>
                <controls:TabbedCommandBarItem Header="Home">
                    <AppBarButton Icon="TwoPage" Label="Diff Images" Click="DiffImagesButton_Click" />
                    <AppBarButton Icon="OpenFile" Label="Open File" Click="OpenFileButton_Click" />
                    <AppBarButton Icon="SaveLocal" Label="Save As" Click="SaveAsButton_Click" />
                </controls:TabbedCommandBarItem>
                <controls:TabbedCommandBarItem x:Name="ViewMenu" Header="View" IsContextual="True" Visibility="Collapsed">
                    <AppBarToggleButton Icon="Stop" Label="View Border" Checked="BorderButton_Checked" Unchecked="BorderButton_Unchecked" IsChecked="True" />
                    <AppBarToggleButton Icon="TouchPointer" Label="Drag" Checked="DragButton_Checked" Unchecked="DragButton_Unchecked" IsChecked="True" />
                </controls:TabbedCommandBarItem>
            </controls:TabbedCommandBar.MenuItems>
        </controls:TabbedCommandBar>

        <ScrollViewer x:Name="ImageScrollViewer" Grid.Row="1"
            ZoomMode="Enabled"
            HorizontalScrollMode="Enabled"
            VerticalScrollMode="Enabled"
            HorizontalScrollBarVisibility="Visible" 
            VerticalScrollBarVisibility="Visible"
            PointerPressed="ScrollViewer_PointerPressed"
            PointerMoved="ScrollViewer_PointerMoved">
            <Border BorderThickness="50" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border BorderThickness="5" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border.BorderBrush>
                        <SolidColorBrush x:Name="ImageBorderBrush" Color="Black" />
                    </Border.BorderBrush>
                    <Grid x:Name="ImageGrid" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Rectangle x:Name="ImageRectangle" PointerMoved="ImageRectangle_PointerMoved" PointerExited="ImageRectangle_PointerExited" />
                    </Grid>
                </Border>
            </Border>
        </ScrollViewer>

        <Grid Grid.Row="2" Background="{Binding Background, ElementName=MainMenu, Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition />
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="PositionTextBlock" Grid.Column="0" VerticalAlignment="Center" />

            <TextBlock x:Name="ImageSizeTextBlock" Grid.Column="2" VerticalAlignment="Center" />

            <Grid Grid.Column="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding ZoomFactor, ElementName=ImageScrollViewer, Mode=OneWay, Converter={StaticResource FloatToPercentageConverter}}" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0, 0, 5, 0" />
                <Slider x:Name="ZoomSlider" Grid.Column="1" Value="{Binding ZoomFactor, ElementName=ImageScrollViewer, Mode=TwoWay}" Minimum="0.1" Maximum="10" StepFrequency="0.1" Margin="5, 0, 5, 0" IsEnabled="False" />
            </Grid>
        </Grid>
    </Grid>
</Page>
